# Shared world map asset

The deployed prototype uses `public/world-map.png`.

The exact source file is already tracked in this repository at:

`assets/mission-000/storyboard-v1/base-map-1920x1080.png`

For a local run, copy it into this directory and rename it to `world-map.png`.
